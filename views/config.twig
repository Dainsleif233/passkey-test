{% extends 'admin.base' %}

{% block content %}
<div class="box">
    <div class="box-header with-border">
        <h3 class="box-title">{{ trans('SysHub\\Passkey::config.title') }}</h3>
    </div>
    <div class="box-body">
        {{ form|raw }}
    </div>
</div>
{% endblock %}