# Passkey 插件安装说明

## 快速安装（推荐）

### 方式一：从 Release 下载（推荐）

1. **下载插件**
   - 从 [Releases](https://github.com/your-username/passkey/releases) 下载最新的 `passkey-vX.X.X.zip` 文件
   - 解压到 Blessing Skin 的 `plugins/` 目录

2. **启用插件**
   - 登录后台管理界面
   - 进入「插件管理」
   - 找到「Passkey」插件并启用

3. **配置插件**
   - 在插件管理中点击「配置」
   - 设置 RP ID（留空则自动使用当前域名）
   - 根据需要调整其他选项

### 方式二：从源码安装

1. **下载源码**
   - 克隆或下载本仓库到本地

2. **安装依赖（必须）**
   ```bash
   cd passkey
   composer install
   ```
   > ⚠️ **这一步是必须的**！没有这一步，插件会报错 "Class 'lbuchs\WebAuthn\WebAuthn' not found"

3. **复制到插件目录**
   - 将整个 `passkey` 目录复制到 Blessing Skin 的 `plugins/` 目录

4. **启用插件**
   - 登录后台管理界面
   - 进入「插件管理」
   - 找到「Passkey」插件并启用

5. **配置插件**
   - 在插件管理中点击「配置」
   - 设置 RP ID（留空则自动使用当前域名）
   - 根据需要调整其他选项

## 详细安装步骤

### 前置要求

- Blessing Skin Server 6.0 或更高版本
- PHP 8.1 或更高版本
- PHP 扩展：`openssl`、`mbstring`
- Composer（用于安装 PHP 依赖）
- 站点必须使用 HTTPS（localhost 除外）

### 步骤 1: 获取插件文件

确保 `passkey` 目录包含以下文件：

```
passkey/
├── package.json
├── composer.json
├── bootstrap.php
├── callbacks.php
├── routes.php
├── assets/passkey.js
├── lang/{en,zh_CN}/*.yml
├── src/Controllers/*.php
├── src/Models/Passkey.php
├── src/Support/*.php
├── views/*.twig
├── README.md
└── LICENSE
```

### 步骤 2: 安装 PHP 依赖

在插件目录中运行：

```bash
cd plugins/passkey
composer install
```

这将安装 `lbuchs/webauthn` 库及其依赖。

### 步骤 3: 启用插件

1. 登录 Blessing Skin 后台管理
2. 导航到「插件管理」页面
3. 找到「Passkey」插件
4. 点击「启用」按钮

启用时，插件会自动：
- 创建 `passkeys` 数据库表（如果不存在）
- 设置默认配置选项

### 步骤 4: 配置插件

1. 在插件管理中，点击「Passkey」插件的「配置」按钮
2. 配置以下选项：

#### RP ID（依赖方 ID）
- **默认值**：留空（自动使用当前域名）
- **说明**：WebAuthn 依赖方 ID，通常是您的域名
- **多域名站点**：必须显式设置为您的主域名

#### RP Name（依赖方名称）
- **默认值**：留空（使用站点名称）
- **说明**：显示给用户的依赖方名称

#### 用户验证
- **选项**：Preferred / Required / Discouraged
- **默认值**：Preferred
- **说明**：认证时是否要求用户验证（如生物识别）

#### 显示登录按钮
- **默认值**：开启
- **说明**：在登录页面显示通行密钥登录按钮

#### 记住登录
- **默认值**：开启
- **说明**：通行密钥登录是否记住会话

#### 每用户最大通行密钥数
- **默认值**：5
- **说明**：每个用户可以注册的最大通行密钥数量

### 步骤 5: 测试插件

1. **注册通行密钥**：
   - 登录用户账号
   - 访问 `user/passkey` 页面
   - 点击「添加新通行密钥」
   - 按照浏览器提示完成注册

2. **测试登录**：
   - 登出当前账号
   - 在登录页面点击「使用通行密钥登录」
   - 选择已注册的通行密钥进行登录

## 故障排除

### 问题 0: "Class not found" 错误

**症状**：启用插件后出现类似以下错误：
```
Class 'lbuchs\WebAuthn\WebAuthn' not found
```

**原因**：未运行 `composer install` 或依赖未正确安装

**解决方法**：
```bash
cd plugins/passkey
composer install
```

如果 composer 未安装，请先安装：
```bash
# 下载 composer
php -r "copy('https://getcomposer.org/installer', 'composer-setup.php');"
php composer-setup.php
php -r "unlink('composer-setup.php');"

# 移动到全局可访问位置（Windows）
move composer.phar C:\php\composer.phar

# 然后运行
composer install
```

### 问题 1: 登录按钮不显示

**可能原因**：
1. 浏览器不支持 WebAuthn
2. 页面未使用 HTTPS
3. 管理员禁用了登录按钮

**解决方法**：
- 使用支持 WebAuthn 的浏览器（Chrome 67+、Firefox 60+、Safari 13+）
- 确保站点使用 HTTPS
- 检查插件配置中的「显示登录按钮」选项

### 问题 2: 无法创建通行密钥

**可能原因**：
1. 未运行 `composer install`
2. 数据库表不存在
3. 达到通行密钥数量上限

**解决方法**：
1. 在插件目录运行 `composer install`
2. 重新启用插件（会自动创建表）
3. 检查并调整「每用户最大通行密钥数」配置

### 问题 3: 登录时提示「认证失败」

**可能原因**：
1. Challenge 已过期（5分钟有效期）
2. 凭据数据无效
3. 服务器时间不同步

**解决方法**：
- 确保服务器时间准确
- 重新尝试登录流程
- 检查浏览器控制台错误信息

### 问题 4: 插件无法启用

**可能原因**：
1. PHP 版本不满足要求
2. 缺少必要的 PHP 扩展
3. 文件权限问题

**解决方法**：
- 检查 PHP 版本（需要 8.1+）
- 确保 `openssl` 和 `mbstring` 扩展已启用
- 检查插件目录的文件权限

## 开发调试

### 启用调试模式

在 `.env` 文件中添加：
```
APP_DEBUG=true
```

### 查看日志

日志文件位于 `storage/logs/laravel.log`

### 语法检查

```bash
# PHP 语法检查
find . -name "*.php" -exec php -l {} \;

# JavaScript 语法检查
node --check assets/passkey.js

# JSON 格式检查
node -e "JSON.parse(require('fs').readFileSync('package.json', 'utf8'))"
```

## 卸载插件

1. 在后台禁用插件
2. 删除 `plugins/passkey` 目录
3. **注意**：数据表不会自动删除，需要手动删除 `passkeys` 表

## 更新插件

1. 备份当前插件目录
2. 替换为新版本文件
3. 运行 `composer update`（如果有依赖变更）
4. 在后台重新启用插件

## 技术支持

如有问题，请查看：
- [README.md](README.md) - 完整文档
- [验收清单.md](验收清单.md) - 功能测试清单
- [test-webauthn.php](test-webauthn.php) - WebAuthn 集成测试

## 许可证

本插件使用 [GPL-3.0-only](LICENSE) 协议。