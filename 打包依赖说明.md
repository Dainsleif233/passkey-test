# 打包依赖说明

## 方案2：将依赖打包到插件中

如果你想让插件"即装即用"，不需要用户运行 `composer install`，可以按照以下步骤操作：

### 步骤

1. **运行 composer install**
   ```bash
   cd plugins/passkey
   composer install --no-dev --optimize-autoloader
   ```

2. **修改 .gitignore**
   移除 `/vendor/` 这一行，让 vendor 目录被包含在版本控制中：

   ```gitignore
   # 实施计划文档，不纳入版本库
   /PLAN.md
   /CONTEXT.md

   # Composer（删除这一行）
   # /vendor/
   composer.lock

   # IDE / 编辑器
   .idea/
   .vscode/
   *.swp

   # 工具缓存与日志
   .php-cs-fixer.cache
   *.log

   # 系统文件
   Thumbs.db
   .DS_Store
   ```

3. **发布插件**
   现在你的插件目录包含 vendor，用户下载后可以直接使用。

### 优缺点

**优点**：
- ✅ 用户下载后直接可用，无需运行 composer
- ✅ 适合分发给不懂技术的用户
- ✅ 避免用户环境缺少 PHP/Composer 的问题

**缺点**：
- ❌ 插件体积增大（lbuchs/webauthn 约 50-100KB）
- ❌ 需要手动更新依赖（无法通过 `composer update` 自动更新）
- ❌ 可能与主站的其他插件产生依赖冲突
- ❌ 不是官方推荐的做法

### 替代方案：发布预编译包

更好的做法是创建一个发布包（release），包含 vendor 目录：

1. 在 GitHub/Gitee 上创建 release
2. 上传包含 vendor 的 zip 包
3. 用户下载 release 包，不需要运行 composer

这样既保持了源码仓库的干净，又方便用户安装。

## 官方推荐做法

根据 [Blessing Skin 插件文档](https://bs-plugin.netlify.app/guide/composer.html)，官方推荐的做法是：

1. 插件仓库**不包含** vendor 目录
2. 在 `.gitignore` 中排除 `/vendor/`
3. 在 README 中说明需要运行 `composer install`
4. 用户在安装插件后运行 `composer install`

这是 PHP 生态的标准做法，类似于 Node.js 的 `npm install`。

## 建议

对于你的 passkey 插件，我建议：

1. **保持标准做法**（方案1）
   - 仓库不包含 vendor
   - README 中清楚说明需要运行 `composer install`
   - 这是官方推荐的方式

2. **如果需要分发给不懂技术的用户**
   - 创建 GitHub Release
   - 上传包含 vendor 的 zip 包
   - 用户下载 release 包即可直接使用

这样既保持了代码仓库的规范，又方便了不同用户的需求。