# Passkey 插件人工验收清单

## 环境准备

1. [ ] 确认 Blessing Skin Server 6.x 已安装
2. [ ] 确认 PHP 8.1+ 已安装，扩展 `openssl`、`mbstring` 已启用
3. [ ] 确认站点使用 HTTPS（localhost 除外）
4. [ ] 将 `passkey` 目录放入 `plugins/` 目录
5. [ ] 在插件目录执行 `composer install`（需要 PHP 和 Composer）
6. [ ] 在后台插件管理中启用 "Passkey" 插件

## 功能测试

### 1. 登录功能

- [ ] 登录页面显示"使用通行密钥登录"按钮（如果浏览器支持 WebAuthn）
- [ ] 点击按钮触发浏览器 WebAuthn 认证器
- [ ] 成功认证后跳转到用户主页
- [ ] 认证失败显示相应错误信息

### 2. 通行密钥管理

- [ ] 访问 `user/passkey` 页面显示管理界面
- [ ] 点击"添加新通行密钥"按钮触发创建流程
- [ ] 输入名称后成功创建通行密钥
- [ ] 新通行密钥出现在列表中
- [ ] 点击重命名按钮可以修改名称
- [ ] 点击删除按钮可以删除通行密钥
- [ ] 删除前有确认对话框

### 3. 管理员配置

- [ ] 访问插件配置页面（后台 > 插件 > Passkey）
- [ ] 可以修改 RP ID 设置
- [ ] 可以修改 RP 名称设置
- [ ] 可以修改用户验证级别
- [ ] 可以切换显示登录按钮开关
- [ ] 可以切换记住登录开关
- [ ] 可以修改每用户最大通行密钥数

### 4. 安全性测试

- [ ] 已登录用户访问登录接口返回错误
- [ ] Challenge 过期后无法使用（等待5分钟后测试）
- [ ] 封禁用户无法通过通行密钥登录
- [ ] 无效的凭据数据返回错误
- [ ] 不存在的通行密钥返回错误

## 边界情况

### 1. 浏览器兼容性

- [ ] Chrome 67+ 正常工作
- [ ] Firefox 60+ 正常工作
- [ ] Safari 13+ 正常工作
- [ ] 不支持 WebAuthn 的浏览器隐藏按钮

### 2. 网络环境

- [ ] HTTPS 环境下正常工作
- [ ] HTTP 环境（localhost）下正常工作
- [ ] HTTP 环境（非 localhost）下按钮隐藏

### 3. 数据一致性

- [ ] 插件停用后数据保留
- [ ] 插件重新启用后数据可用
- [ ] 插件删除后数据保留
- [ ] 用户删除账号时清理相关通行密钥

## 错误处理

### 1. 依赖缺失

- [ ] 未运行 `composer install` 时显示友好错误
- [ ] 数据库表不存在时显示友好错误
- [ ] WebAuthn 库未安装时显示友好错误

### 2. 认证失败

- [ ] 无可发现凭据时提示"未找到通行密钥"
- [ ] 认证器拒绝认证时提示相应错误
- [ ] 网络错误时显示通用错误信息

## 性能测试

- [ ] 登录页面加载时间 < 2秒
- [ ] 通行密钥管理页面加载时间 < 2秒
- [ ] 认证流程响应时间 < 5秒

## 文档完整性

- [ ] README.md 包含完整安装说明
- [ ] 配置选项有详细说明
- [ ] FAQ 解答常见问题
- [ ] 协议声明明确（GPL-3.0-only）

## 代码质量

- [ ] 所有 PHP 文件通过 `php -l` 语法检查
- [ ] JavaScript 文件通过 `node --check` 语法检查
- [ ] JSON 文件格式正确
- [ ] YAML 文件格式正确
- [ ] 无敏感信息泄露（密钥、token 等）

## 部署检查

- [ ] 插件目录权限正确（可读写）
- [ ] 缓存已清除（如果需要）
- [ ] 会话配置正常
- [ ] CSRF 保护正常工作

## 最终验收

- [ ] 所有测试项通过
- [ ] 功能符合需求文档
- [ ] 安全设计符合要求
- [ ] 用户体验良好
- [ ] 错误处理完善

---

**验收人**：________________

**验收日期**：________________

**备注**：________________